export const $ = <T extends HTMLElement>  (selector: string) => {    return document.querySelector(selector);}export const $$ = <T extends HTMLAllCollection> (selector: string) => {    return document.querySelectorAll(selector);}export const createElement = <T extends  HTMLElement> (tagName: string, className?: string, content?: string) => {    const newElement:HTMLElement = document.createElement(tagName);    if(className) newElement.setAttribute("class", className);    if(content) newElement.innerHTML = content;    return newElement;}