import {$} from "../../utils/pulign.js";import {toggleScrolling} from "../../utils/helper.js";const appModal = <HTMLDivElement> $(".app-modal");const appModalBody = <HTMLDivElement> appModal.querySelector(".app-modal__body");const appModalOverlay = <HTMLDivElement> appModal.querySelector(".app-modal__overlay");const toggleFlex = ():void => {    appModal.classList.toggle("d-flex");}export const toggleModal = ():void => {    toggleFlex();    toggleScrolling();}appModalOverlay.addEventListener("click", toggleModal);